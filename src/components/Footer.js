import "./Footer.css"

export default function Footer(){
    return(<div className = "foot">
        <nav className= "Number">
            <a href="#">About us</a>
            <a href="#">Contact</a>
            </nav>
            <nav className="media">
            <a href="#">Twitter</a>
            <a href="#">Facebook</a>
        </nav>
    </div>
    )
}